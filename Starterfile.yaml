tools:
  npm:
    optional: False
    script: scoop install npm || brew install npm || sudo apt-get npm

modules:
  path.module-express:
    name: express
    source:
      git: git@github.com:Start-Out/path.module-express.git
    scripts:
      init: cd ./express && npm install

